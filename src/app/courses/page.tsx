import { Placeholder } from "@/components/Placeholder";

export const metadata = { title: "Courses · KIIT Archive" };

export default function CoursesPage() {
  return (
    <Placeholder
      title="Courses"
      caption="View the top free courses and certifications students can enroll in."
    />
  );
}
